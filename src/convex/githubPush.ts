/**
 * Deprecated after launch cleanup.
 * The source snapshot routing and token-based GitHub publishing path have been removed
 * from the production app surface. This module remains as a stub to avoid accidental
 * reimports while the repository history is still being corrected.
 */
export const disabled = true;
