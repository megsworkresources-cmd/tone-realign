# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright component tests (browser binaries + CT vite cache + results)
playwright/.cache
test-results

# Secrets & local env (managed in the Keys/API keys UI — never commit)
.env.keys
.env.local

# Archives (exported source snapshots)
*.zip
*.tar.gz

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
